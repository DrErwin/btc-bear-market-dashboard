# v0.3.1 — 实现记录

> 状态：功能已在本地实现并通过验收，尚未提交、推送或部署。

## 1. 已完成内容

- 15 个非 STH-MVRV 指标共用一份图表／状态阈值配置。
- 指标状态显示实际触发的最深命名档位，不再统一写成笼统的“进入观察区”。
- v0.3.0 证据编译器改为读取当日快照阈值，MVRV、Puell 和 AVIV 不再保留另一套硬编码判断值。
- STH-MVRV 图表水平参考线已清空，“阈值线”开关在该指标下隐藏。
- STH-MVRV 的 `5%分位`、`1.5σ` 和 `1.5·MAD` 三档状态阈值改为每日使用最新可用历史重新计算。
- 三条 STH-RP 战术价格曲线继续保留并随动态阈值更新。
- 固定成功包、失败回退包和无回退包已重新生成。

## 2. 本地验收

- `python -m pytest -q tests/acceptance`：`110 passed`。
- `cd dashboard && npm run build`：通过；仅保留既有 Vite bundle 大小提示。
- `python tests/acceptance/run_acceptance.py`：通过，输出 `ACCEPTANCE PASS: success, fallback, no-fallback, responsive, keyboard, restricted-language`。
- 自动检查确认 15/15 个非 STH-MVRV 指标完全对齐，STH-MVRV 状态阈值为 3 条、图表阈值为 0 条。

## 3. 2026-07-28 验收快照

以下数值只记录当天结果，不是固定参数：

| 项目 | 结果 |
|---|---|
| STH-MVRV 当前值 | `0.9384476698` |
| 5% 分位／观察区 | `0.7492245464` |
| 1.5σ／深度压力区 | `0.7315351535` |
| 1.5·MAD／极端压力区 | `0.7211303869` |
| 当前 STH-MVRV 状态 | `未进入观察区` |
| STH-MVRV 图表水平参考线 | `0 条` |
| 当前整体阶段 | `尚未进入熊底观察期` |

本次数据刷新使用 `--mock-ai`，用于验证数据计算、阶段边界和页面流程；它不代表真实远程模型调用已经通过。

## 4. 与 v0.3.0 的关系

- v0.3.0 的指标角色、数据质量闸门、证据简报、`allowed_stages` 和 AI 输出限制继续使用。
- v0.3.1 只更换这些判断所读取的阈值来源，不撤销 v0.3.0。
- 当前数据包的 schema/config 标识仍为 `0.3.0`，因为本轮尚未执行正式发布。发布 v0.3.1 前，应同步更新版本标识、重新生成三份数据包并完成线上核验。

## 5. 验收证据

当前复核图片位于 `artifacts/review-evidence/v0.3.0/`。该目录最初由 v0.3.0 验收入口建立，随后也保存了本轮图表复核图片。正式发布 v0.3.1 时，应生成独立的 `artifacts/review-evidence/v0.3.1/`，避免继续混用版本目录。

## 6. 发布边界

- 本轮修改尚未形成新的 Git 提交。
- 尚未推送 GitHub，也未部署到 Cloudflare。
- 线上版本仍以 README 记录的 v0.2.4 为准。
- 不提供交易建议、价格预测、仓位或杠杆建议。
