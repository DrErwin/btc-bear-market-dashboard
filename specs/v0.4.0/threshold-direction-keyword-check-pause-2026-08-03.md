# 阈值方向关键词检查暂停设计（2026-08-03）

## 背景

2026-08-02 的基础指标已经更新，但 GLM-5.2 生成的当天分析在三次尝试后仍未通过语义校验，系统因此保留 2026-08-01 的上一份成功分析。最终错误显示，方向关键词扫描把 Seller Exhaustion Constant 和 Reserve Risk · 周期的文字识别为“高于”，而当天两项指标触发的都是 `below` 阈值。

当前公开记录没有保存三次被拒绝的完整原文，无法进一步区分模型确实写反方向与否定句被关键词扫描误判。为先恢复日更，本次临时暂停这一项方向关键词扫描。

## 已确认范围

- 暂停根据“高于、低于、高位、低位、上穿、下穿、升入、跌入”等词判断阈值方向是否写反。
- 保留未触发指标、不可用或过期指标、未触发档位、虚构日期、状态变化、分轴数据就绪、相关证据重复计票、外部信息和输出结构等检查。
- 不修改 GLM-5.2 Prompt、三次生成机制、压力状态或筑底状态词汇，也不由机器替代 AI 选择状态。
- 三次生成后仍有其他校验错误时，继续安全回退上一份成功分析。

## 验收边界

以 `services.ai.semantic_validator.validate_analysis_semantics()` 为公开测试入口：

1. 一份其他方面有效、仅含阈值方向关键词的分析应通过。
2. 同一入口仍应拒绝把未触发指标写成支持证据的分析。
3. 现有 AI 语义契约测试全部通过。

## 恢复条件

后续只有在能够按具体字段和句子识别否定语境，并保存可审计的失败片段后，才重新启用方向文字检查；不得恢复当前的整段关键词扫描。
